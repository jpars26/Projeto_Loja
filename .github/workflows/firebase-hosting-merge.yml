# Arquivo: .github/workflows/deploy.yml

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Checkout do repositório
        uses: actions/checkout@v3

      - name: 🔧 Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Instalar dependências
        run: |
          cd frontend
          npm ci  # 🔹 Instala todas as dependências de forma confiável

      - name: 🧪 Rodar testes
        run: |
          cd frontend
          npm test -- --testPathPattern=src/tests  # 🔹 Garante que só os testes dentro de `src/tests` rodem

      - name: 📦 Construir o projeto
        run: |
          cd frontend
          npm run build  # 🔹 Gera os arquivos de produção na pasta `build`

      - name: 🚀 Deploy para Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PRODUCAO_LOJA }}
          channelId: live
          projectId: producao-loja
